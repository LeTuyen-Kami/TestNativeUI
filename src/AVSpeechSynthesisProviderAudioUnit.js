import React from 'react';
import {Text, View} from 'react-native';

const AVSpeechSynthesisProviderAudioUnit = props => {
  return (
    <View
      style={{
        flex: 1,
        backgroundColor: 'rgba(55,55,55,0.5)',
      }}>
      <Text>Hellop</Text>
    </View>
  );
};

export default AVSpeechSynthesisProviderAudioUnit;
